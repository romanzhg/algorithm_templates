Notes

Running median:
1. Two heaps, a min heap and a max heap, and record the size of each heap, maintain heaps at each
new number. The size of two heaps can differ by at most 1.

Find the smallest positive number missing from an unsorted array:
Value of the smallest positive number missing must be smaller or equal to the array size.
Put all negative nubmers to front and use the input array as a number presented/not presented check.